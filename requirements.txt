# Minimal dependencies for the Streamlit app to run
# Updated 2025-11-05: Fixed pinecone package name (cache bust)
streamlit==1.50.0
openai>=1.30.0,<2
PyPDF2>=3.0.1,<4
requests>=2.31.0,<3

# RAG + persistence stack (kept optional by import guards in app.py)
langchain>=0.2.11,<0.3
langchain-openai>=0.1.8,<0.2
langchain-community>=0.2.11,<0.3
langchain-text-splitters>=0.2.2,<0.3
# Prefer Chroma for persistent vector DB; FAISS as lightweight fallback
chromadb>=0.5.0,<0.6
faiss-cpu>=1.7.4,<2
pandas>=2.2.0,<3

# Enterprise Document Intelligence (Primary - High Accuracy)
google-cloud-documentai>=2.20.0  # Google Document AI for advanced OCR and layout understanding
pinecone==5.0.0  # Pinecone vector database (NOTE: renamed from pinecone-client)
google-cloud-storage>=2.10.0  # GCS for Document AI file handling

# Advanced PDF parsing and document intelligence (Fallback)
pdfplumber>=0.11.0  # Advanced PDF parsing, table extraction, layout analysis
pypdf>=3.17.0  # Modern PyPDF replacement with better text extraction
camelot-py[cv]>=0.11.0  # Robust table extraction from PDFs
pytesseract>=0.3.10  # OCR for scanned documents and images
pdf2image>=1.17.0  # Convert PDF pages to images for OCR
Pillow>=10.0.0  # Image processing
opencv-python>=4.8.0  # Computer vision for image analysis
tabulate>=0.9.0  # Table formatting and display
python-magic>=0.4.27  # File type detection
openpyxl>=3.1.2  # Excel file handling for exports

# Additional AI/ML libraries for intelligent understanding
numpy>=1.24.0  # Numerical computing
scikit-learn>=1.3.0  # Machine learning utilities
regex>=2023.0.0  # Advanced regex for pattern matching in technical documents
rank-bm25>=0.2.2  # BM25 algorithm for hybrid search (keyword + semantic)
